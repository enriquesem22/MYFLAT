# Configuración para desplegar Myflat en Netlify.
[build]
  command = "npm run build"
  publish = "dist"

# React Router (SPA): redirige todas las rutas a index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
